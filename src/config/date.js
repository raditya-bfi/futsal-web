const date = {
  daily: {
    format: 'YYYY-MM-DD',
    viewFormat: 'D MMMM YYYY',
    mode: ['year', 'month', 'day'],
  },
  monthly: {
    format: 'YYYY-MM',
    viewFormat: 'MMM YYYY',
    mode: ['year', 'month'],
  },
  yearly: {
    format: 'YYYY',
    viewFormat: 'YYYY',
    mode: ['year'],
  },
}

export default date
